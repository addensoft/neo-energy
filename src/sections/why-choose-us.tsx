import { Section } from "@/components/section";

/**
 * Why Choose NEO Energy — differentiators checklist + certifications,
 * inspired by the reference homepage's "Why Choose NEO Energy?" section.
 * Not implemented this sprint — shell only.
 */
export function WhyChooseUs() {
  return (
    <Section id="why-choose-us">
      {/* Why Choose NEO Energy content: differentiators + certifications — future sprint */}
    </Section>
  );
}
