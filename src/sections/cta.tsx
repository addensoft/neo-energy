import { Section } from "@/components/section";

/**
 * Chapter 8 — The Close (CTA). Creative Direction §2, §4.
 * Chapter 0's held hero frame returns, full circle. No-fade hold reused
 * (§4: reserved for exactly two beats on the page — this is the second).
 * Not implemented this sprint — shell only.
 */
export function CTA() {
  return (
    <Section id="cta">
      {/* Chapter 8 content: returning hero frame + assessment CTA — Sprint 2+ */}
    </Section>
  );
}
